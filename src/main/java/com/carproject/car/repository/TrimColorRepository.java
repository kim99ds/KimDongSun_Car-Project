package com.carproject.car.repository;

import com.carproject.car.entity.TrimColor;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TrimColorRepository extends JpaRepository<TrimColor, Long> {
}
